---
codd:
  node_id: design:extract:architecture-overview
  type: design
  source: extracted
  confidence: 0.7
  last_extracted: '2026-03-30'
  depends_on:
  - id: design:extract:system-context
    relation: derives_from
    semantic: technical
  - id: design:extract:cli
    relation: aggregates
    semantic: technical
  - id: design:extract:config
    relation: aggregates
    semantic: technical
  - id: design:extract:extractor
    relation: aggregates
    semantic: technical
  - id: design:extract:generator
    relation: aggregates
    semantic: technical
  - id: design:extract:graph
    relation: aggregates
    semantic: technical
  - id: design:extract:hooks
    relation: aggregates
    semantic: technical
  - id: design:extract:implementer
    relation: aggregates
    semantic: technical
  - id: design:extract:parsing
    relation: aggregates
    semantic: technical
  - id: design:extract:planner
    relation: aggregates
    semantic: technical
  - id: design:extract:propagate
    relation: aggregates
    semantic: technical
  - id: design:extract:root
    relation: aggregates
    semantic: technical
  - id: design:extract:scanner
    relation: aggregates
    semantic: technical
  - id: design:extract:synth
    relation: aggregates
    semantic: technical
  - id: design:extract:validator
    relation: aggregates
    semantic: technical
  - id: design:extract:verifier
    relation: aggregates
    semantic: technical
---
# Architecture Overview (Extracted)

> Auto-generated by `codd extract`. Treat this as a review draft of the current architecture.

## System Overview

**Language**: python

## Architectural Layers

### Presentation

- `cli` — Exposes routes, handlers, or CLI entrypoints### Application

- `generator` — Coordinates use cases or service-level workflows- `implementer` — Coordinates use cases or service-level workflows- `planner` — Coordinates use cases or service-level workflows- `verifier` — Coordinates use cases or service-level workflows### Domain

- No modules classified into this layer.
### Infrastructure

- `config` — Implements parsing, extraction, scanning, or adapters- `extractor` — Implements parsing, extraction, scanning, or adapters- `graph` — Implements parsing, extraction, scanning, or adapters- `hooks` — Implements parsing, extraction, scanning, or adapters- `parsing` — Implements parsing, extraction, scanning, or adapters- `propagate` — Implements parsing, extraction, scanning, or adapters- `root` — Defaulted to infrastructure because no higher-level cues were detected- `scanner` — Implements parsing, extraction, scanning, or adapters- `synth` — Defaulted to infrastructure because no higher-level cues were detected- `validator` — Implements parsing, extraction, scanning, or adapters### Shared

- No modules classified into this layer.

## Layer Violations

- `scanner` (Infrastructure) imports `generator` (Application) via from codd.generator import _load_wave_artifacts; from codd.generator import _load_wave_artifacts
## Module Dependency Graph

```
cli -> config
cli -> extractor
cli -> generator
cli -> hooks
cli -> implementer
cli -> planner
cli -> propagate
cli -> scanner
cli -> validator
cli -> verifier
extractor -> config
extractor -> parsing
extractor -> synth
generator -> config
generator -> scanner
hooks -> config
hooks -> scanner
hooks -> validator
implementer -> generator
implementer -> scanner
parsing -> extractor
planner -> config
planner -> generator
planner -> validator
propagate -> graph
propagate -> scanner
scanner -> generator
scanner -> graph
scanner -> parsing
synth -> extractor
verifier -> config
```




## External Dependencies

- `ast`
- `click`
- `click>=8.0`
- `codd`
- `copy`
- `fnmatch`
- `graphql`
- `graphql-core>=3.2.0`
- `hcl2`
- `jinja2`
- `jinja2>=3.1.0`
- `python-hcl2>=7.0.0`
- `pyyaml>=6.0`
- `shlex`
- `shutil`
- `tomli as tomllib`
- `tomli>=2.0.1; python_version < '3.11'`
- `tomllib`
- `tree-sitter-java>=0.22`
- `tree-sitter-python>=0.22`
- `tree-sitter-python>=0.25.0,<0.26.0`
- `tree-sitter-typescript>=0.22`
- `tree-sitter-typescript>=0.23.0,<0.24.0`
- `tree-sitter>=0.22`
- `tree-sitter>=0.25.0,<0.26.0`
- `tree_sitter`
- `tree_sitter_python`
- `tree_sitter_sql`
- `tree_sitter_typescript`
- `yaml`

## Cross-Cutting Concerns

### Configuration

- `config`

## Entry Points & Deployment

- Build scripts: codd

## Build Metadata

**Manifest**: `pyproject.toml`

Runtime dependencies: pyyaml>=6.0, click>=8.0, jinja2>=3.1.0, tomli>=2.0.1; python_version < '3.11'
Development dependencies: graphql-core>=3.2.0, python-hcl2>=7.0.0, tree-sitter>=0.25.0,<0.26.0, tree-sitter-python>=0.25.0,<0.26.0, tree-sitter-typescript>=0.23.0,<0.24.0, tree-sitter>=0.22, tree-sitter-python>=0.22, tree-sitter-typescript>=0.22, tree-sitter-java>=0.22
