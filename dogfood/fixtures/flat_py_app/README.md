# flat_py_app (synthetic zoo fixture)

A generic flat-layout Python repo (no `src/`, modules at root) used to exercise
CoDD's deterministic extractor against a naive directory convention. Synthetic
and product-agnostic. Not a real project.
