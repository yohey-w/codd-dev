// Intentional match for the declared forbidden_evidence pattern
// (FORBIDDEN_TOKEN_[A-Z0-9]+). This is a synthetic placeholder, not a real
// secret — it exists only so the negative_space check produces a hit.
export const placeholder = "FORBIDDEN_TOKEN_ABC123";
